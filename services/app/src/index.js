import React from 'react';
import ReactDOM from 'react-dom';

const title = 'Zk-SNARK Non-Fungible Tokens';

ReactDOM.render(
  <h1>{title}</h1>,
  document.getElementById('app')
);

module.hot.accept();